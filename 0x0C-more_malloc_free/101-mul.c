#include < stdlib.h>
#include "main.h"
#include <stdio.h>

/**
 
 *
 *
 *
 *
 *
 */
int checkd(char *c)
{
	int a = 0; 

	while (c[a])
	{
		if (c[a] < '0' || c[a] > '9'
				return (0);
		i++;
	}
	return (1);
}
