#include "main.h"
/**
 * _abs - Prints the absolute integer
 *
 * @r: collects number
 *
 * Return: Returns the absolute integer
 *
 */
int _abs(int r)
{
	if (r >= 0)
	{
		return (r);
	}
	else
	{
		return (r * (-1));
	}
}
