#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always (0)
 *
 */
int main(void)
{
	int baseTen = 0;

	while (baseTen <= 10)
	{
		putchar(baseTen);
		baseTen++;
	}
	putchar('\n');
	return (0);
}
